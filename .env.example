# ─────────────────────────────────────────────
# YOUR VOICE — Environment Variables
# Copy this file to .env and fill in your values
# ─────────────────────────────────────────────

# Supabase (required for both modes)
# Find these in your Supabase project → Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# OpenAI (required for local dev mode only)
# In production (Netlify), this key is stored as a Supabase secret — never put it here
OPENAI_API_KEY=sk-your-openai-key

# ─────────────────────────────────────────────
# PRODUCTION ONLY (Netlify deployment)
# ─────────────────────────────────────────────
# Set this in Netlify → Site → Environment Variables
# Leave it unset (or remove it) for local dev — the app will fall back to Express
#
# VITE_API_BASE=https://your-project-ref.supabase.co/functions/v1
